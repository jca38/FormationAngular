import { ColorRoleDirective } from './color-role.directive';

describe('ColorRoleDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorRoleDirective();
    expect(directive).toBeTruthy();
  });
});
