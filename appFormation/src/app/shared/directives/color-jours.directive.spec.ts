import { ColorJoursDirective } from './color-jours.directive';

describe('ColorJoursDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorJoursDirective();
    expect(directive).toBeTruthy();
  });
});
